local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

test:
  service: Disk
  root: <%= Rails.root.join("storage", "test") %>

production:
  service: Disk
  root: <%= ENV.fetch("RAILS_STORAGE_PATH", Rails.root.join("storage", "production")) %>
