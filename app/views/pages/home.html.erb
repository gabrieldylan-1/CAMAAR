<section class="hero">
  <div class="card">
    <h1><%= t("pages.home.hero_title") %></h1>
    <p class="lead"><%= @message %></p>
    <p>
      Execute <code>docker compose up --build</code> para inicializar o servidor Rails e depois execute
      <code>docker compose run --rm web bundle exec cucumber</code> para rodar os cenários de BDD.
    </p>
  </div>
</section>
